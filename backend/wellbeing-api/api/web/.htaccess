Options -Indexes

RewriteEngine on

# Send would-be 404 requests to Yii
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.php

# Security headers
Header always set X-Content-Type-Options "nosniff"
